version: 0.2

phases:
	install:
		runtime-versions:
			python: 3.11
		commands:
			- python -m pip install --upgrade pip
			- python -m pip install -r requirements.txt
	build:
		commands:
			- python -m py_compile app.py

artifacts:
	files:
		- '**/*'
	discard-paths: no
